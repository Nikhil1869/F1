services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./fastf1_cache:/app/fastf1_cache
      - ./precomputed:/app/precomputed
    env_file:
      - .env
    environment:
      - FLASK_ENV=development
